package net.velocity.test.services;

import org.springframework.stereotype.Service;

@Service
public class PrimeService {
	
	/**
	 * Check if parameter is a natural number greater than 1 that has no positive divisors other than 1 and itself
	 * It should fail if a negative number is passed as a parameter
	 * @param number
	 * @return
	 */
	public boolean isPrime(int number){
		return false;
	}

}
